package com.kotlin

/*
 * 코틀린은 표준 자바 컬렉션 클래스를 사용하지만 더 많이 확장된 기능을 사용할 수 있다.
 *   * 확장된 기능은 확장 함수로 정의된다.
 * 함수에 전달하는 인자에 이름을 붙일 수 있고, 함수 파라미터의 기본값을 정할 수 있다.
 * 함수를 최상단에 정의할 수 있어서 유틸리티 클래스가 필요하지 않다.
 * public static final == const
 * 확장함수: 어떤 클래스의 멤버 메서드인것처럼 호출할 수 있지만 그 클래스 밖에 선언된 함수
 * 자바에서 명시적으로 클래스의 상속을 금지하는 final class 가 코틀린의 기본 클래스 개념이다.
 *
 */
